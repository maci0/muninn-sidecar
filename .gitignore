# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
cover.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Application binaries (anchored to repo root so they don't match cmd/* source dirs)
/msc
/testmsc

# Profiling and coverage artifacts
*.prof
coverage.html

# Distribution directory
dist/

# Environment files
.env
.env.*
!.env.example

# OS/Editor specific
.DS_Store
.idea/
.vscode/
*.swp
/msc-bench
/msc-eval
/msc-qa

# Antigravity CLI (agy) drops this in the working dir
.antigravitycli/

# Local agent/session state
.claude/
